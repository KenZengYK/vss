unit clrlegendformu;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls;

type
  Tfrmclrlegend = class(TForm)
    Label1: TLabel;
    Label2: TLabel;
    Label3: TLabel;
    procedure FormClose(Sender: TObject; var Action: TCloseAction);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  frmclrlegend: Tfrmclrlegend;

implementation

{$R *.dfm}

procedure Tfrmclrlegend.FormClose(Sender: TObject;
  var Action: TCloseAction);
begin
  action:=cafree;
  frmclrlegend:=nil;
end;

end.
