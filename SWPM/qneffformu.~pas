unit qneffformu;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls, Buttons;

type
  Tfrmqneff = class(TForm)
    Label1: TLabel;
    Label2: TLabel;
    Label3: TLabel;
    Label4: TLabel;
    BitBtn1: TBitBtn;
    procedure BitBtn1Click(Sender: TObject);
    procedure FormClose(Sender: TObject; var Action: TCloseAction);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  frmqneff: Tfrmqneff;

implementation

{$R *.dfm}

procedure Tfrmqneff.BitBtn1Click(Sender: TObject);
begin
  close;
end;

procedure Tfrmqneff.FormClose(Sender: TObject; var Action: TCloseAction);
begin
  action:=cafree;
  frmqneff:=nil;
end;

end.
